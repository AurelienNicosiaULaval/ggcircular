% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vonmises-mixture.R
\name{fit_vonmises_mixture}
\alias{fit_vonmises_mixture}
\title{Fit a mixture of von Mises distributions}
\usage{
fit_vonmises_mixture(
  x,
  k = 2,
  weights = NULL,
  axial = FALSE,
  init = c("kmeans", "spaced"),
  nstart = 1,
  init_params = NULL,
  kappa_max = 10000,
  min_component_weight = 1e-08,
  max_iter = 200,
  tol = 1e-08,
  na.rm = TRUE,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric vector of angles in radians.}

\item{k}{Number of mixture components.}

\item{weights}{Optional non-negative observation weights.}

\item{axial}{Should data be treated as axial, modulo \code{pi}?}

\item{init}{Initialization method, either \code{"kmeans"} or \code{"spaced"}.}

\item{nstart}{Number of EM starts. The fit with the largest log-likelihood
is retained.}

\item{init_params}{Optional list or data frame with initial \code{proportions},
\code{mu} and \code{kappa} values.}

\item{kappa_max}{Maximum fitted concentration. This caps nearly degenerate
components.}

\item{min_component_weight}{Minimum relative component weight before a
component is reinitialized.}

\item{max_iter}{Maximum number of EM iterations.}

\item{tol}{Convergence tolerance on the log-likelihood.}

\item{na.rm}{Should missing values be removed?}

\item{seed}{Optional random seed used for initialization.}
}
\value{
An object of class \code{ggcircular_vonmises_mixture}.
}
\description{
Fits a finite mixture of von Mises components using an expectation
maximization algorithm. For axial data, the algorithm fits doubled angles and
returns component means on the original modulo-\code{pi} scale.
}
\examples{
fit <- fit_vonmises_mixture(wind_directions$direction, k = 2)
tidy_circular(fit)
}
\seealso{
Other circular distributions: 
\code{\link{stat_vonmises}()},
\code{\link{stat_vonmises_fit}()},
\code{\link{stat_vonmises_mixture}()}
}
\concept{circular distributions}
