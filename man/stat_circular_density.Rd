% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-circular-density.R
\name{stat_circular_density}
\alias{stat_circular_density}
\title{Circular density statistic}
\usage{
stat_circular_density(
  mapping = NULL,
  data = NULL,
  geom = "line",
  position = "identity",
  ...,
  method = c("kernel", "vonmises"),
  bw = NULL,
  adjust = 1,
  n = 512,
  axial = FALSE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, geom, position, show.legend, inherit.aes}{Standard ggplot2
layer arguments.}

\item{...}{Additional arguments passed to the layer.}

\item{method}{Density method. Currently \code{"kernel"} and \code{"vonmises"} both use
a von Mises kernel estimator.}

\item{bw}{Optional circular bandwidth. It is interpreted as \code{1 / sqrt(kappa)}.}

\item{adjust}{Multiplicative adjustment applied to \code{bw} or to the automatic
bandwidth scale.}

\item{n}{Number of grid points.}

\item{axial}{Should the data be treated as axial, modulo \code{pi}?}

\item{na.rm}{Should missing values be silently removed?}
}
\value{
A ggplot2 layer. Computed variables are \code{x}, \code{density}, \code{scaled},
\code{count}, \code{n}, \code{bw} and \code{kappa}.
}
\description{
Estimates a smooth circular density using a von Mises kernel. The density
wraps around the origin, avoiding the boundary artifacts of a linear kernel
density estimate.
}
\examples{
ggplot2::ggplot(wind_directions, ggplot2::aes(x = direction)) +
  stat_circular_density()
}
\seealso{
Other circular density layers: 
\code{\link{geom_circular_density}()}
}
\concept{circular density layers}
