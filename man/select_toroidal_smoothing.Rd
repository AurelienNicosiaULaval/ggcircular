% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/directional-smoothing.R
\name{select_toroidal_smoothing}
\alias{select_toroidal_smoothing}
\title{Select smoothing parameters for toroidal conditional density}
\usage{
select_toroidal_smoothing(
  theta,
  phi,
  kappa_theta_values = c(6, 8, 12, 16, 24, 36),
  kappa_phi_values = kappa_theta_values,
  n_folds = 5,
  seed = NULL
)
}
\arguments{
\item{theta}{Conditioning angles in radians.}

\item{phi}{Response angles in radians.}

\item{kappa_theta_values}{Candidate concentrations for the conditioning
angle.}

\item{kappa_phi_values}{Candidate concentrations for the response angle.}

\item{n_folds}{Number of cross-validation folds.}

\item{seed}{Optional random seed for fold assignment.}
}
\value{
A data frame ranked by decreasing mean held-out log score. The
returned object has class \code{directional_smoothing_selection}.
}
\description{
Ranks candidate smoothing parameters by K-fold conditional log-likelihood for
\code{f(phi | theta)}.
}
\examples{
dat <- simulate_toroidal(n = 80, scenario = "diagonal", seed = 1)
select_toroidal_smoothing(
  dat$theta, dat$phi,
  kappa_theta_values = c(8, 12),
  kappa_phi_values = c(8, 12),
  n_folds = 2,
  seed = 1
)
}
\seealso{
Other directional smoothing helpers: 
\code{\link{select_cyl_smoothing}()}
}
\concept{directional smoothing helpers}
