% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-rose.R
\name{geom_rose}
\alias{geom_rose}
\title{Rose diagram layer}
\usage{
geom_rose(
  mapping = NULL,
  data = NULL,
  stat = "rose",
  position = "identity",
  ...,
  bins = 30,
  binwidth = NULL,
  boundary = 0,
  closed = TRUE,
  area = FALSE,
  normalize = c("count", "density", "proportion"),
  axial = FALSE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping, data, position, show.legend, inherit.aes}{Standard ggplot2 layer
arguments.}

\item{stat}{Statistical transformation, usually \code{"rose"}.}

\item{...}{Additional arguments passed to the layer.}

\item{bins}{Number of bins over the circular period.}

\item{binwidth}{Optional bin width in radians. If supplied, \code{bins} is ignored
after the number of bins is inferred from the period.}

\item{boundary}{Lower boundary for the first bin.}

\item{closed}{Included for API compatibility. Values on the upper period
boundary are wrapped into the first bin.}

\item{area}{If \code{TRUE}, radial heights are square-root transformed so that
visual area is closer to the selected frequency scale.}

\item{normalize}{Which scale should be used for the computed radial \code{y}
value: counts, densities or proportions.}

\item{axial}{Should angles be treated as axial, modulo \code{pi}?}

\item{na.rm}{Should missing values be silently removed?}
}
\value{
A ggplot2 layer.
}
\description{
\code{geom_rose()} is a convenience wrapper around \code{\link[=stat_rose]{stat_rose()}} using a bar
geometry. It is designed to be used with \code{\link[=coord_circular]{coord_circular()}} or
\code{ggplot2::coord_polar()}.
}
\examples{
ggplot2::ggplot(wind_directions, ggplot2::aes(x = direction)) +
  geom_rose(bins = 16) +
  coord_circular()
}
\seealso{
Other rose diagram layers: 
\code{\link{stat_rose}()}
}
\concept{rose diagram layers}
