% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/directional-smoothing.R
\name{select_cyl_smoothing}
\alias{select_cyl_smoothing}
\title{Select smoothing parameters for circular-linear conditional density}
\usage{
select_cyl_smoothing(
  theta,
  x,
  kappa_values = c(6, 8, 12, 16, 24, 36),
  h_values = NULL,
  n_folds = 5,
  seed = NULL
)
}
\arguments{
\item{theta}{Conditioning angles in radians.}

\item{x}{Linear response.}

\item{kappa_values}{Candidate circular concentration values.}

\item{h_values}{Candidate linear bandwidth values. If \code{NULL}, a small grid is
built around the rule-of-thumb bandwidth used by \code{\link[=estimate_cyl_density]{estimate_cyl_density()}}.}

\item{n_folds}{Number of cross-validation folds.}

\item{seed}{Optional random seed for fold assignment.}
}
\value{
A data frame ranked by decreasing mean held-out log score. The
returned object has class \code{directional_smoothing_selection}.
}
\description{
Ranks candidate smoothing parameters by K-fold conditional log-likelihood.
}
\examples{
dat <- simulate_cylindrical(n = 80, seed = 1)
select_cyl_smoothing(
  dat$theta, dat$x,
  kappa_values = c(8, 12),
  h_values = c(0.3, 0.5),
  n_folds = 2,
  seed = 1
)
}
\seealso{
Other directional smoothing helpers: 
\code{\link{select_toroidal_smoothing}()}
}
\concept{directional smoothing helpers}
